import { ExamSubjectForESSUpdateVM } from './exam-subject-for-essupdate-vm';

describe('ExamSubjectForESSUpdateVM', () => {
  it('should create an instance', () => {
    expect(new ExamSubjectForESSUpdateVM()).toBeTruthy();
  });
});
